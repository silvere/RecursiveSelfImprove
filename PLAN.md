---
title: "PLAN — 任务队列（系统可自改）"
date: 2026-08-10
draft: false
tags: [rsi, plan]
summary: "围绕 GOAL v2（内容流水线）重排：先打通最小端到端闭环，再提质量、再补数据回流。"
---

# 任务队列

> 格式：`- [ ] T-NNN 任务描述 ｜验收方式`。完成后移入"已完成"段并附证据。
> 2026-08-10 晚场按 GOAL v2 重排（人的 INBOX 指令①）。排序原则：**先让一篇文章走完全程，再谈每天一篇**——链路不通时优化质量是空转。

## 关键路径（v2 最小闭环）

选题（wtqn）→ 成稿（aiwriter2 七刀）→ 转换（AIWriter/posts 格式）→ 同步（wechat-sync CI）→ 记录（LEDGER 文章产出记录）

已知断点：**AIWriter2 的产物在 `AIWriter2/articles/<slug>/`，而 wechat-sync 只认 `AIWriter/posts/<date>/<slug>/article.html`**（证据：`~/.claude/commands/wechat-sync.md` Step 2 的 find 命令写死该路径；`ls AIWriter2/articles/` 为 `2026-07-12-ai-learning` 等目录）。这是 T-010 要修的第一个洞。

## 待办


- [ ] T-028 **`cover-check` 在实际运行里拦不住同步——配图 CI 成功会自动触发同步 CI，中间没有人工窗口**（L-048 本场实测：08-22 这篇的同步 run 32537586928 在会话跑 cover-check 之前就已 in_progress）｜现状等于"判定跑了、但它的结论到不了它该拦的那道门"，本次侥幸判定通过所以无害｜两条候选修法：①把 cover-check 挪进配图 CI 内部、判失败即不写 posts.json；②让同步 CI 开工前读 cover-check 的结论文件｜验收（按策略 10）：造一篇题图与前一篇重复的样本，**同步 CI 必须真的没跑**，而不只是 cover-check 打印了拒绝｜**排序：属 CI 流水线类，排在每日文章之后**
> 以下 5 条全部来自 **2026-08-16 第 1 次周审**（外部审计员，报告全文 `docs/audit/2026-08-16-week1.md`，摘录 LEDGER `W-001`~`W-012`）。
> 排序依据审计员的一句判断：**"它对自己的日志比对自己的产品认真得多"** —— 故内容质量类排在记账机制类之前。

- [ ] T-024 **修 `record_run end` 漏行**（L-039，第五次同坑）｜08-18 晚场干完了活（journal 19:36 + commit 389160a 19:37）却只落了 `phase=start` 行，没有 end 行；前四次见 L-028。**本次第一次暴露下游代价**：新加的 `fail_streak()` 因此在真实日志上多数一场（报 11，真值 10），**任何基于 `session-runs.log` 的统计精度上限都被它锁死** ｜做法：完整读一遍 run.sh 从 `wait $CLAUDE_PID` 到 `exit $RC` 的所有退出路径，找出哪条路径会绕过 `record_run "$RC"`（重点查 `set -e`／trap／提前 exit／notify 阻塞）｜验收：构造该路径复现漏行，修复后同场景必落 end 行，且 `fail_streak` 在真实日志上给出 10 ｜**注意：这是记账机制类，按周审排序原则，排在内容质量类之后——不要因为它"看起来好修"就插队到文章前面**
- [ ] T-022 **C1/C2 计数只认本流水线当日新产出**（W-003、W-010④）｜08-13 那篇是 08-08 就已进草稿箱的旧稿换目录重传，却计入 C1 分子且被记为"端到端首篇" ｜验收：产出记录该行标注真实首同步日期并从计数中移除，仪表盘 C1 如实回落（预期 4→2），且 C2 分母同步修正 ｜**注意：这条不是"撤勾"，是修计数口径；C3/C4 撤勾按审计员意见留给人裁决**
- [ ] T-024 **破"标签由本场自己贴"的自我确认闭环**（W-008）｜验收判定被自产条目点亮已 2 次，L-022 只修了匹配位置没修判断权 ｜验收：策略 7 的跨场复核范围从"命令输出对不对"扩到"**这条该不该算**"，含策略自带的可判定指标（本场问题 1 已独立发现同一缺口）
- [ ] T-025 **单场耗时改为可复算，预算超支入账**（W-009）｜本周所有"约 X 分钟"全是自估无计时证据，而 **H4"首次验证通过"这个结论就建在自估上**；超预算 ≥4 次从未入账，其中一次直接导致当日文章被放弃 ｜验收：`session-runs.log` 的 start/end 时间戳差值自动写进 journal 收尾段，H4 用真实数据重新判定

## 进行中

（**清空于 2026-08-22 pm**：今日篇已闭环，本日四场全跑通，无在途任务。

**给 08-23 s1（01:30）的排序建议**：①**文章仍是唯一第一优先级**，`pick_topics.py --date 2026-08-23` 生成候选后按 L-037 必答题先判适配；②**本场刚给 ⑤a 加了 R-02 相对时间基准检查**（L-050），明天那篇写草稿时如果被它拦住，改法是**在同一句里写出起止绝对日期并当场做减法**，不要只写跨度、更不要把它删掉了事；③T-024／T-022／T-028 仍未动，按周审排序原则排在内容之后；④**08-23 是周日，晚场自动升级为第 2 次周审**——第 1 次周审的 5 条建议（T-021~T-025）目前只闭环了 T-021 和 T-023，周审时要如实报这个完成率。）

## 已完成（本场新增）

- [x] **写作能力日拱一卒：R-02 相对时间基准改机检**（2026-08-22 pm）｜动机是当天证据刀的必修 1（把距截止的**剩余** 19 天当成**已过去**的 19 天，方向反了；同形错误 08-16 那篇也犯过）｜**做法刻意不新开脚本**：并进已有的 `AIWriter2/tools/vague_quantifier_check.py`（⑤a 闸门），因为它已挂在唯一的门禁点上、已有 `05a-vague-check.log`、已被 `run_daily.sh` 第 1b 步与 `--verify` 复核 ｜产物：AIWriter2 commit `f9c064e`（脚本 + `skills/aiwriter2.md` ⑤a 段同步）｜**证据**：真样本双向——今天的 `05-draft.md` 原句命中 `L53「19 天里」` RC=5，同篇改后 `final.md` 同一句 **0 处 RC=0**；`--verify` 对今日文章仍 RC=0（门禁未破）；⑤ 量级词旧规则回归仍命中 3 处 ｜**假阳性治理见 L-050**：全量 9 篇终稿首版命中 8 处、逐条核对 6 处错杀（75%），加三类例外后只剩 2 处且均为真该改的；期间前瞻词表的单字"会"误匹配本仓最高频词"会话"，**一度让闸门在自己的真样本上失效**

- [x] **把策略 11 从"会话拿眼睛扫"换成 commit 门禁**（2026-08-22 s3，本场自发现，非计划内——s2 交接原本要求的执行方式就是 L4）｜产物：`system/pipeline/evidence_grade.py` + `system/hooks/pre-commit` + `git config core.hooksPath system/hooks` ｜**证据三组**：正常态 `evidence_grade.py` → `检查 48 条 / L1 34 ｜ L4 0 ｜ 无结论句 14` RC=0；扰动实测 5 组（S1 纯自评→L4 拒／S2 带 RC+commit→L1 放／S3 无结论词→不检查／S4 三条混合分别判 L2·L3·L4 只拒 L4／**真样本 L-047 抹掉锚点后 L1→L4、RC 0→1**）；端到端 E3 未动 LEDGER 的 commit 放行（`baf0a65`）、E1 追加一条纯自评条目 → **真 commit 被真拦住 RC=1** ｜**本条最值钱的是差点上线的那一版**：初版路径正则报 4 条裸奔，逐条核对 **3 条是误判**（假阳性 75%），据此补出策略 10 缺的对称一半——门禁的第二种死法是错杀，见 **L-049** ｜遗留：`core.hooksPath` 是本机 git 配置不进版本控制，换机器需重跑一次（已写进 hook 文件头注释）


- [x] T-027 **复验 ⑤a 机检闸门真的把 R-01 打到 0** ｜✅ **2026-08-22 s2 判定通过**｜证据（机器可判口径，数字全部由脚本自算、不经会话之手）：`vague_quantifier_check.py --verify /Users/jingweisun/Code/AIWriter2/articles/2026-08-22-unused-inventory` → **退出码 0**，输出 `✓ ⑤a 机检闸门已执行：草稿阶段拦下 2 处，跑了 2 次，末次 0 处`｜草稿阶段两处命中（「那几天」「只需要几秒」）逐条改成可查证精确值或不带量的动作后重跑 RC=0，**七刀证据刀复查 R-01 命中 0 处**（08-21 那篇为 2 处，T-026 因此判未通过）｜**闸门确有拦截力而非恒真**：同篇内四十六 / 六 / 35→41 / 四天 / 四篇 / 十一天 / 5 天等精确数值**全部放行**，与 L-044 定的判别线（拦模糊、放精确）一致｜**同场还暴露了闸门管不到的一类**：`06-review.md` 里我自己凭印象写的"草稿加粗 9 处"（实测 14）——机检只扫正文不扫审稿记录，这类 L4 自评由新落的 STRATEGY 策略 11 接管（L-047）｜遗留欠账不追改：08-17 那篇 final.md 的"我读了很多遍"仍在（已同步，改了读者也看不到差异）

- [x] **今日篇（08-21）同步草稿箱闭环** ｜✅ **2026-08-21 s3 复核确认**：media_id=`-Eu-2F7MukrOEiQGhnmJq7ezGbLlkIyIib9fxJCLg1iJQImUM82XWRRbTxkJXdxI`，marker 落在 origin/main commit `648e292`（10:05）｜**首次成功不是本场做成的**——是定时 run `32438484873`（synced_at 02:04Z=北京 10:04）；**人在收到 s2 那条含具体 IP 的人话通知后约 2 小时内加了白名单**，策略 9 写死的 08-23 默认值未触发 ｜本场 `gh run rerun 32429558883` 也 success，但已是空操作（marker 已存在）｜**这条给策略 9 的正向证据**：闸一判"只有人能做"判对了，而人话通知里带上**具体 IP + 后台路径**是它两小时内被执行的原因——上一次同因失败（08-20 共四次 run）没通知人，就一直没人动

- [x] T-021 **题图质量进入验收视野**（W-006，原最高优先级）｜✅ **2026-08-21 s3 达成**，见 LEDGER **L-042** ｜产物：`system/pipeline/cover_check.py`（指纹=git blob sha，封面口径复刻 `_pick_cover`）+ `run_daily.sh cover-check` 子命令（退出码 4=题图重复，决策落 `workspace/pipeline-runs.log`）｜**三组证据齐全**：①存量回跑 origin/main 130 篇 0 重复，且对修复前 ref `6608f14^` 回跑命中 1 组 3 篇（证明非恒真，L-021 口径）；②正常态取值（策略 8）今日篇 ✓ RC=0；③**扰动实测（策略 10）**用临时索引造扰动树 `a8426b22a5e1`，把今日篇题图换成 08-17 那张 → 判定拒 RC=1 ｜**位置的判断是这条任务真正的产出**：不能放进 ship 门禁（那时目录里还没有图，判定恒过），必须放在配图 CI 成功之后、同步之前，且读 origin/main 不读本地工作区

- [x] **修 LEDGER 条目错位**（本场自发现，非计划内）｜✅ L-036~L-041 六条教训被追加在 `## 简报送达记录` 之后，对 `gen_dashboard.py` 的 C3 判定完全不可见（41 条里 6 条＝14.6%）｜已移回 `条目` 段末尾，正文一字未改 ｜证据：移动前 `条目段 35/#流水线 10/带→调整 9`，移动后 `41/11/10`，仪表盘 C3 证据行由 9/8 → 11/8（含本场两条新条目），`gen_dashboard.py` RC=0 ｜**留下守恒体检**：`条目段内 [L- 行数 == 全文 [L- 行数`，本场实测 43==43 ｜详见 L-043

- [x] T-023 **人的依赖项加超时与默认值**（W-007）｜✅ **2026-08-17 pm 达成**：落成 **STRATEGY 策略 9**，两道闸——闸一"只问人独占的信息"（C2 判定口径这类系统自己的职责不得推给人）、闸二"同一问题追问 3 场未答即按预设默认执行并显著标注，默认值必须在第一次提问时就写出" ｜**第二个独立证据源（本场新得，强于审计员原证据）**：s2 已把三个追问改写成完全无代号的大白话重发，人 08:49 回复全文九字「个人。／其他不知道你在说啥。」——**只答了唯一那个"只有人能看后台"的问题**，说明失败模式是选题不是措辞 ｜**当场执行不留悬空**：C2 口径改由系统自己定（并入 T-022）、职业规划 6 项输入停止追问（报告已按未答口径交付）、今晚唯一新提问自带默认值（08-20 前未答则 `published` 全按 `n`，仪表盘标注"人未答，按默认口径"）｜带"→ 调整"的教训 **L-035** ｜反向体检 `grep -c '人未答，按默认' journal/*.md LEDGER.md` 应随每个超时问题同步增长

- [x] T-014 **数据回流通道建成 + 结论定案**（GOAL 验收第 4 条）｜✅ **2026-08-17 pm**：人答「个人。」→ 个人主体订阅号无法微信认证，`datacube/*` 权限位**永久封死**（结论从推断升级为已验证，`docs/data-return-feasibility.md` 新增第七节）｜通道实体建成：`workspace/metrics/wechat-reads.csv`（5 篇在册，含 date/slug/title/media_id_prefix/published/reads/likes/recorded_at/note）+ `workspace/metrics/README.md` 填写说明 ｜**判定同步收紧且代价自负**：`gen_dashboard.py` 新增 `c4_data_return()`，只认表里**至少一行有实测 reads**，不再认自产文档存在（W-008／T-024 的自我确认闭环）——**C4 因此当场由 ✓ 回落为 ✗** ｜四场景实测：现状 0 行有读数→False／伪造 1 行实测→True（"1/5 篇，最近抄录 2026-08-20"）／published=y 但没抄数→False／文件不存在→False，改完文件已比对还原 ｜`python3 system/gen_dashboard.py` RC=0 ｜**剩余唯一缺口在人**：从公众号后台抄 5 个数（约 1 分钟）

- [x] T-013 **一键化 `system/pipeline/run_daily.sh`**：把 T-010 的手工步骤串成一条命令，失败即中止并报错到简报 ｜验收：连跑 2 天，会话只需执行一条命令 + 人工审校 ｜**进度（08-15 s3 建成，08-16 s1 复核）**：脚本已落地（128 行，prep/check/ship 三子命令），自测六项全过、本场复核五项相符一项未复核（见 journal/2026-08-15-s3.md 表格）｜**剩余就是"用它跑真文章"**：08-16、08-17 两天的成稿必须走 `run_daily.sh ship`，而不是会话手敲步骤——自测通过不等于验收通过，验收口径是连跑 2 天真文章 ｜**第 1 天达成（08-16 s2）**：今日文章全程由 `run_daily.sh ship` 交付，会话未手敲任何一步——`check` → `✓ RC=0`；`ship` 依次产出 `AIWriter/posts/2026-08-16/agent-permission/article.md`(3717 字符) + `article.html`(17,080 字符) → worktree 提交 `bf472bf` → 推送 main → CI 配图（run 31915333957 `完成：1 处已填充`）→ CI 同步草稿箱（run 31915386837 `汇总：同步 1 | 失败 0`，media_id=-Eu-2F7MukrOEiQGhnmJq-vmj1Y01vXERTMo8VTDZJMyCotUztVqKC9hkWwAgKYq）｜**✅ 验收达成（2026-08-17 s2，第 2 天）**：《「没救了」这三个字，混着三件完全不同的事》全程 `run_daily.sh ship`，会话未手敲任何一步——`check` → `✓ RC=0`；`ship` → `article.md`(2,671 字符) + `article.html`(16,078 字符) → AIWriter commit `2e0e3e7` → push main → CI 配图（run 31979698196 success）→ CI 同步草稿箱（run 31979756430 `汇总：同步 1 | 去重补标记 0 | 跳过 0 | 失败 0`，media_id=-Eu-2F7MukrOEiQGhnmJq-6D2W3bwNyzMVvlkR90_UR4YT036kiinsvmRoOep_xE）｜**连跑 2 天真文章（08-16、08-17）的验收口径已满足**

## 已完成

- [x] T-020 **run_daily.sh 把七刀设为必经步骤**（08-15 s3 完成，08-16 s1 复核并追认）｜**比原验收更强**：原定"第 3 步后调 review_trace.py"，实际把门禁做成了 **ship 的第 1 步**——不通过就不产出任何 posts 文件、不碰 AIWriter 仓库，而不是产出后再回滚 ｜验收证据（08-16 s1 复跑，非 s3 自述）：正例 `check <2026-08-15-falsification>` → `✓` RC=0；负例（只有 final.md 的空壳）→ `✗ 缺：选题依据；论点竞技场结论；七刀审稿记录` RC=3；`ship` 负例 RC=3 且无产出；逃生开关 `--force-no-trace` 不带理由 → RC=2 拒绝执行 ｜每次门禁决策 pass/fail/bypassed 一视同仁落 `workspace/pipeline-runs.log` ｜教训见 L-024

- [x] T-012 **选题入口接 wtqn**：每日从 wtqn 问题库取 3 条候选，落地为 `workspace/topics/YYYY-MM-DD.md`（含来源链接），供当日成稿选用 ｜验收：连续 2 天有候选文件且每条可回溯到 wtqn 原始条目（服务 GOAL 验收第 2 条"可溯源"）｜**进度（08-14 s2）：第 1 天已达成** — `workspace/topics/2026-08-14.md` 3 条候选均带 `talk_id/source_url/question_idx`，当日文章即取自候选 1；库真实位置是 Obsidian `Silvere/AI-Articles/Question-Bank/`（236 条），**不在 `~/Code/wtqn/data`**（该目录不存在）｜**进度（08-14 s3）：已脚本化** — `system/pipeline/pick_topics.py`，08-15 候选已自动生成（`workspace/topics/2026-08-15.md`，主候选 3 + 备选 8），会话只需补写"论点角度"一段；**剩余：08-15 当场确认该文件可用并成稿，即满足"连续 2 天"闭环**｜已知空洞：抓取管线自 2026-04-27 无新增，`sources/podcast`、`sources/substack` 均 0 条

｜**验收达成（2026-08-15 s2）**：连续 2 天有候选文件且逐条可回溯——`workspace/topics/2026-08-14.md`（当日文章取候选 1）与 `workspace/topics/2026-08-15.md`（脚本自动生成，主候选 3 + 备选 8；当日文章取候选 1，talk_id=e2ea9f797c40 / question_idx=1，回溯字段完整写进 `AIWriter2/articles/2026-08-15-falsification/01-brief.md`）｜**选题判断仍由会话做且本场真的做了**：3 条主候选里 2 条（生态数据 AI、太空数据中心）无一手材料、题材不适配，明写淘汰理由后取候选 1——脚本给池子，会话给判断，这个分工经两天验证成立

- [x] T-019 **两日专题：AI 时代 3-5 年职业规划分析报告**（人 08-13 下达，08-15 晚场交付，每场 ≤50% 预算）｜进度：证据采集已完成（`workspace/career-2026/evidence-code.md` 从 ~1080 条自有 commit 倒推精力分布与放弃模式；`evidence-writing.md` 从 456 篇写作倒推主题聚类与未闭合命题）｜下一步：待人答复 6 项关键输入（职级与平台资源／收入底线与风险承受度／心流与耗竭分布／期望的被需要方式／家庭地域约束／外部筹码），先写 2~4 条路线的骨架与"关键假设/第一年验证动作/放弃信号"，人答复后交叉打分 ｜**进度（08-14 s2）：骨架已完成** — `workspace/career-2026/routes-draft.md` 4 条赌注互斥的路线（A 评测标准定义权／B 独立验收方法论／C 内容基础设施产品化／D 脱钩写作资产），每条含可证伪假设＋第一年验证动作＋可观察放弃信号，按"人未答复"口径**刻意不排序**，末段写明 6 项待答输入各取值如何影响排序 ｜下一步：08-15 成文交付；**最该追问的一项已定位**——"Agent 轨迹系统设计稿是合规不允许对外，还是根本没试过"，它单独决定 A 与 B 的相对位置 ｜验收：报告存 Obsidian `Silvere/AI-Articles/`（日期前缀），简报给摘要 ｜**交付达成（2026-08-15 s1）**：报告成文 `Silvere/AI-Articles/2026-08-15-AI时代3-5年职业规划分析报告.md`（23,814 字节，6 节：三条硬约束／四条路线／默认推荐／失败模式横向对比／六项输入翻转表／最该先答的一个问题） ｜**人的 6 项输入至今全部未答**，故按"未答"口径成文：第 3 节给出**仅依赖作品证据**的默认推荐（A 主线 60% + B 最小对冲 25% + D 长期对冲 15%，C 降级为一个 2 个月强制判定点），并列出"这份推荐在什么情况下是错的"三条；第 5 节写明每项输入取何值时推荐翻转 ｜**第一年只有 4 个动作算数**（各路线唯一能证伪其核心假设者，均 3 个月内出结论）：A-3 走对外发布合规流程／B-2 向 3 个外部项目提 PR/issue／C-3 AIWriter2 修好或关停／D-1 三个月 6 篇第一人称 ｜遗留给晚场：简报给摘要 + 第三次追问 6 项输入，重点问"轨迹系统设计稿没发出去，是合规不允许还是没试过"（该问题单独决定主线是 A 还是 B）

- [x] T-015 **七刀审稿记录归档规范 + 可机器检查**（2026-08-14 s3）｜验收达成：`docs/review-trace-spec.md` 定义存放路径（`AIWriter2/articles/<日期-slug>/` 一个目录装全）与最小三件套（选题依据 `01-brief.md`／竞技场结论 `03-arena.md`／七刀记录 `06-review.md`，各 ≥200 字节且含内容特征）+ 终稿；检查器 `system/pipeline/review_trace.py` 可单篇跑也可 `--ledger` 全量跑；`gen_dashboard.py` 的 C2 判定改为调用同一函数（命令行与仪表盘不会两套口径）｜**存量回跑证据**：`review_trace.py --ledger` → `✗ 2026-08-14（审稿记录未指向文章目录）/ ✓ 2026-08-13 / 可溯源 1/2 篇`，RC=1；仪表盘 C2 证据行同步显示"可溯源 1/2 篇；2026-08-14 审稿记录未指向文章目录" ｜**顺带清掉 L-010 的欠账**：`check_ledger_sections()` 把"段落不存在"与"段落为空"分开，缺段打警告并让 gen_dashboard 非零退出（负例实测：删掉 `## 文章产出记录` 标题后返回 `['文章产出记录']` 并告警）｜遗留 → T-020

- [x] T-012-脚本化（子项，2026-08-14 s3）｜**脚本化证据**：`system/pipeline/pick_topics.py` 实测 `--date 2026-08-15` → `✓ workspace/topics/2026-08-15.md（主候选 3 + 备选 8，库 200 条，已排除 6 键）`；去重实测 `grep -c` 今日已用的 3 个 talk_id = **0**，不会重复选题；文件已存在时跳过并非零退出（防覆盖会话补写的角度判断）；库缺失/候选不足时明确报错而非产出空文件 ｜**刻意的分工**：脚本只做机械部分（定位库、解析、去重、排序、抄全回溯字段），"选哪条、什么角度"仍由会话判断 ｜**已知局限**：importance 高分条目里技术/科学类偏多（08-15 的 3 条主候选有 2 条是生态数据、太空数据中心，不适合本号），故备选池留到 8 条供会话替换——纯分数排序不等于选题适配 **数据回流可行性结论**（2026-08-14 s2，早于 08-16 截止）｜**结论：官方 API 路径不可行，阻塞点是账号资质不是工程** ｜验收达成：`docs/data-return-feasibility.md` 给出明确二选一结论 + 被拒证据 + 代理方案 ｜**实测对照证据（同一 access_token 下）**：`cgi-bin/draft/count` → `{"total_count": 96}` 成功，而 `datacube/getarticlesummary`／`getarticletotal`／`getusersummary`／`getusercumulate`／`freepublish/batchget` 全部 `{"errcode":48001,"errmsg":"api unauthorized"}`——凭证有效、链路通畅，唯独数据类权限位没开，据此判定为**未认证订阅号**（官方文档载 `getarticlesummary` 限「仅认证」）｜代理方案：主通道人工周度抄录后台阅读数 → `workspace/metrics/wechat-reads.csv`（5-8 分钟/周，延迟 ≤7 天）；辅通道 Pages 镜像站接 Cloudflare Web Analytics（一次性 20 分钟，此后 0 人工，但与公众号阅读的相关性**标注为未验证推测**，需 8 周双轨数据检验）｜**已实测否决**：GitHub `repos/.../traffic/views` 返回 `count=0`，统计的是仓库页而非 Pages 站点，不能当代理指标 ｜**顺带查实**：`wechat.py::publish_draft()` 在生产链路中从未生效过——系统一直无发布权限，群发始终由人手动点击（与 GOAL 约定一致）｜**待人回答一个问题**（不影响结论，只影响未来是否值得花 300 元/年）：该公众号是企业还是个人主体？个人主体无法微信认证，此路永久封死；企业主体认证后 API 全自动打通，改造约 1 小时。主体类型 API 读不到，需登录后台「设置与开发 → 账号详情」看一眼

- [x] T-018 **修调度挂死与锁死连锁**（2026-08-14 s1，事故驱动，非计划内）｜验收达成：看门狗实测本该跑 120s 的进程 4s 被杀且 RC=142；锁的 4 场景用生产函数体实测全对 ｜证据：`system/run.sh`（超时改 wall-clock 看门狗 + 锁加 start 时间戳与强制接管）；事故根因与可判定指标见 L-017 ｜**注意：改动对下一场（07:30）起生效，本场自身仍跑在旧版超时机制上**

- [x] T-017 **修 WebP 配图阻塞**（2026-08-13 s2）｜验收达成：`.wechat-sync.json` 的 `uploaded_image_count: 1`（原为 0），新 media_id=-Eu-2F7MukrOEiQGhnmJqxAuSYZI65ADisZwY68OQ3cJiX2JJWo6Fgs7tMCn8yNj ｜证据：CI run 31651452747 `强制模式：删除旧草稿 → ✓ media_id=... 图片1 → 汇总：同步 1 | 失败 0`；AIWriter commit 0cb9cdc（Accept 头 + 魔数兜底 + 存量图转 JPEG）、e2af4ee（--force 越过本地 marker）｜根因与两条教训见 L-015 / L-016
- [x] T-010 **端到端跑通第一篇**（2026-08-13 s1）｜证据：LEDGER"文章产出记录"首行 media_id=-Eu-2F7MukrOEiQGhnmJq7hfaBuqStdLXmtRD1EfGDqSvcAXDjVRAgvwb3tBLPrM；CI run 31624378522 输出 `汇总：同步 1 | 失败 0`；AIWriter commit bd8bd44（文章）→ 120d482（配图）→ 3ef5c6c/后续（封面修复）｜实测耗时：约 75 分钟（超单场预算 2.5 倍，全部超支花在 WebP 配图问题的 4 次 CI 往返上，见 L-012）
- [x] T-011 **转换桥 `system/pipeline/to_posts.py`**（2026-08-13 s1）｜证据：脚本 109 行，实测对 `AIWriter2/articles/2026-08-08-hobby` 产出 article.md(3,960 字符)+article.html(16,814 字符)，占位符正则匹配数 1，产物通过 wechat-sync 全流程并拿到 media_id ｜遗留：封面生成尚未纳入脚本（本次手工 sips 转换），并入 T-017

- [x] T-001 rsi.jerryai.cn 上线 ｜证据：2026-08-10 经 tunnel ingress + route dns 部署（A-001/A-002 批准当日执行），DoH 验证 HTTP 200；本地服务 cn.jerryai.rsi-dashboard（127.0.0.1:8906），备用镜像 silvere.github.io/RecursiveSelfImprove
- [x] T-002 完善仪表盘：验收标准自动打勾（v1 版）｜证据：gen_dashboard.py `acceptance_auto()`；v1 目标停用后该逻辑于 2026-08-10 晚场被 T-016 重写
- [x] T-003 演练介入闭环 ｜证据：人 2026-08-10 12:33 与 13:38 两次经飞书下达指令 → inbox_listener 实时写入 human/INBOX.md → 会话执行（第一条升级为 GOAL v2，commit b16b649；第二条路由为 A-003 提案，见本日 -pm journal）
- [x] T-004 简报送达计数器 ｜证据：gen_dashboard.py `delivery_streak()`，6 组边界用例通过（见 L-002）
- [x] T-016 重建仪表盘验收判定以匹配 GOAL v2 四条标准 ｜证据：gen_dashboard.py `acceptance_auto()` 改为读 LEDGER"文章产出记录"段：C1 连续产出天数≥14、C2 可溯源篇数/总篇数、C3 带"→ 调整："证据的选题/质量教训数≥8、C4 数据回流通道文件存在性；`python3 system/gen_dashboard.py` 退出码 0，当前如实显示 0/4（尚无产出记录）
- （建设期任务见 docs/superpowers/plans/ 与 git 历史）
