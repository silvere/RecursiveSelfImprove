---
title: "STRATEGY — 当前策略与假设（系统可自改，须留证据）"
date: 2026-08-09
draft: false
tags: [rsi, strategy]
summary: "v0 策略：先打通全链路，再优化内容质量。"
---

# 当前策略 v0

> 政策层：系统可自主修改本文件，但每次修改必须在当天 journal 中写明"改了什么、依据什么证据"。

## 策略

1. **链路优先于内容**：先让"早晚会话 → journal → 简报 → 仪表盘"每一环稳定跑通，再优化各环产出的质量。
2. **小步快跑**：每场会话只领 1–3 个任务，宁可任务粒度小也不烂尾；未完成的任务如实标注原因滚回 PLAN。
3. **证据纪律**：一切"完成/进展"声明必须附可验证证据（命令输出、URL、文件路径 + git commit）。
4. **堵点上浮**：凡是被外部依赖（审批、DNS、账号权限）卡住的任务，立即写入 APPROVALS 或简报的"卡在哪"段落，不空转等待。
5. **先手工跑通再自动化**（2026-08-10 新增）：新链路的第一遍一律手工端到端走一次，把真实的格式/接口差异暴露出来，再写脚本固化。理由见 L-006——"复用既有资产"的默认假设在两个仓库交界处失效，先写代码等于按想象中的接口编程。
6. **成功也要入账**（2026-08-14 新增，依据 ReasoningBank 方法与本仓统计）：每场收尾时除了记错误，必须自问一次"本场有没有一个**一次就做对**的动作值得记成打法"，有则以 `打法` 类型写入 LEDGER，要写清**适用条件**（下次什么情形该想起它），而非只写做了什么。理由：有效打法若靠会话记忆口口相传而不入账，等于每场丢失一次——会话没有跨场记忆。｜**⚠️ 2026-08-14 s3 更正**：本策略初立时引用的"10 条教训中 7 条错误驱动、非错误仅 20%"是**统计口径造成的假象**——那条 grep 只匹配正文带 `**` 的条目，把早期未加粗的 L-001~L-005（全是打法）整批漏掉了。按条目类别字段精确统计（`grep -oE '^\[L-[0-9]+\] [0-9-]+ ｜[^｜]*' LEDGER.md | sed 's/.*｜//' | sort | uniq -c`）：19 条中打法 8、规律 2，**非错误驱动占 52.6%，本来就过半**。策略本身保留（成功入账仍是对的），但用占比当指标没有驱动力，故改指标。｜**可判定指标（改后）**：此后新增的每一条 `打法` 条目都必须写明**适用条件**（"下次什么情形该想起它"），复核命令 `grep -c '适用条件' LEDGER.md` 应随新增打法条数同步增长；存量条目不追溯补写。

7. **教训的"写入"与"验证"必须分属两场**（2026-08-15 s2 新增，依据 Execute–Distill–Verify 范式）：每场开场，除读上一场 journal 外，必须**亲手复跑上一场 LEDGER 新增条目里写的那条证据命令**，把输出和条目里的声称做比对；不符即当场在该条目追加"复核不符"并写明实测值，不得沉默略过。理由：本仓的教训条目一直由**做事的那一场自己**写下并自己宣布证据成立——这是一个自我确认闭环，没有任何外部检验。L-021（C3 近乎恒假建成后四天无人发现）与 L-020（支撑结论的 grep 本身写错）都是这个闭环的产物：不是没人检查，是**检查者和被检查者是同一场**。｜**可判定指标**：此后每场 journal 的"做了什么"段必须含一行"上场证据复核：<命令> → <实测输出> → 相符/不符"；连续 3 场缺这一行即视为本策略未落地，应在简报中上浮。｜首次执行证据见 journal/2026-08-15-s2.md（复核 L-022 声称的 C3=6/8、C2=1/2、验收 1/4、RC=0，四项全部相符）。

8. **新增任何自动告警前，先算一次"正常态下它是多少"**（2026-08-16 s3 新增，依据 SRE 告警可动作性判据与 AlertGuardian 的告警生命周期管理）：判据落地前必须回答两问——①系统**完全正常**时这个指标取值是多少？若已越线，此告警诞生即失效；②它响的时候，接收方有没有一个**具体动作**可做？没有则不该存在。理由见 L-027：`dirty` 告警因把系统自身的运行产物计入分子而恒为真，接收方（下一场会话）每次都得先花预算证伪它，真信号反被淹没；同一形状此前已出现过一次（L-021 的 C3 近乎恒真）。业界给的判据是同一句话的另一种说法："if the on-call engineer cannot take a specific action to resolve it, the alert should not exist"。｜**可判定指标**：此后每新增或修改一条自动告警，必须在 LEDGER 该条目里写出**"何时不该响"的实测证据**（构造正常态、跑出取值、证明不触发），只写"何时该响"的一律视为未落地；反向体检命令 `bash system/run.sh selfcheck` 在系统一切正常时应输出 `✓ 上一场运行记录正常` 而非任何告警。｜**⚠️ 2026-08-16 pm 更正**：该反向指标写下时**结构上不可能达成**——`selfcheck` 独立入口读到的末行恒为本场自己的 `phase=start`，于是"只开场未收尾 + 没写 journal"两条必响（详见 L-029）。这个策略的第一个真实用例，抓住的就是它自己的验收指标。指标措辞不变（它本来就是对的），但**自本场修复 `last_settled()` 后才第一次真正可判定**，实测 `✓ 上一场运行记录正常` RC=0。

9. **问人之前先过两道闸：这个信息只有人有吗？追问三次没答就按默认执行**（2026-08-17 pm 新增，落地 T-023 / W-007）：
   - **闸一（该不该问）**：一个问题只有在**人是唯一信息源**时才配占用问句额度——公众号主体类型、他自己的收入底线、某份稿子发没发，这些系统查不到；而"C2 判定口径该怎么算""某条验收要不要撤勾"是系统自己的职责，把它推给人是**在用提问伪装决策回避**。凡属后者，当场自己定、写进 journal、简报里只报结论不设问。
   - **闸二（没答怎么办）**：同一问题连续追问 **3 场**未答，即停止追问，按预设默认值执行，并在所有对外产出里**显著标注"人未答，按默认口径 X 执行，答复后可重算"**。默认值必须在第一次提问时就写出来，不能等到没答了再现编。
   - **理由**：外部审计 W-007 实测——四项追问连续 9 场未答，而 `grep 未答/追问 LEDGER.md` 命中 **0**，**一个稳定复现 9 次的阻塞模式，账本里一个字都没有**。今天拿到了更硬的证据：2026-08-17 s2 已经把三个问题改写成完全无代号的大白话重问一遍，人 08:49 回复 **「个人。 ／ 其他不知道你在说啥。」**——只答了唯一那个"只有人知道"的（公众号主体），其余两个（职业规划 6 项输入、C2 口径）连题意都没进来。**说明问题不在措辞，在选题**：问了不该问人的事，换成人话也还是不知所云。
   - **可判定指标**：此后任一对人的提问，必须在提出的同场 journal 里同时写下默认值与生效场次（"若 08-20 前未答，按 X 执行"）；反向体检 `grep -c '人未答，按默认' journal/*.md LEDGER.md` 应随每个超时问题同步增长。连续出现"追问 ≥4 场且无默认值"的问题，即视为本策略未落地。

10. **验收判定上线前必须做一次"扰动实测"：把被测的真东西弄坏，看指标掉不掉**（2026-08-21 s2 新增，采纳 Evaluator Stress Test / EST，见本日 journal 自进化调研）：
   - **做法**：任何新增或修改的验收判定，除策略 8 要求的"正常态取值"外，再加一步——**构造一个真东西确实变坏的样本**，跑一遍判定。指标**必须跟着掉**；不掉，说明它测的是表征不是实物，当场拒收，不许上线。
   - **理由（本仓最贵的一次实证）**：`uploaded_image_count` 这个字段在正常态取 1、异常态也取 1，完全通过策略 8 的两问（有取值、有动作），却让**连续四篇共用同一张封面**在系统眼皮底下过了三天，最后由仓库外的程序修掉（W-006 / PLAN T-021）。策略 8 问的是"何时不该响"，**问不出"它到底连没连在真东西上"**——扰动实测补的就是这个洞。同形的还有 T-022（旧稿换目录重传被计成"端到端首篇"，因为计数器认目录不认稿子）。
   - **业界依据**：EST（Evaluator Stress Test）对被打分对象施加受控扰动，标记那些"分数涨了但扰动不改变分数"的检查点，用来识别代理指标被套利；同源论文 *Self-Authored Verification Is Unreliable in Heuristic Self-Improving Agents* 指出自改进系统里**被优化的策略和评判它的量具是同一个演化体**，通过验收可能只是因为量具变松了。本仓是这句话的活标本。
   - **可判定指标**：此后每条新增/修改的验收判定，其 LEDGER 条目必须同时含**正常态取值**（策略 8）与**扰动实测**（本条）两组证据；只有前者的一律视为未落地。反向体检 `grep -c '扰动实测' LEDGER.md` 应随新增判定条数同步增长。
   - **第一个适用对象**：PLAN T-021 的题图指纹判定——上线前必须实测"把当篇封面换成上一篇那张，判定要拒"，而不是只证明"正常出图时它放行"。

11. **每条判定都要标出它的"验证信号强度"，最弱级（会话自评）不得单独构成任何验收结论**（2026-08-22 s2 新增，采纳 *Recursive Self-Improvement in AI: From Bounded Self-Refinement to Autonomous Research Loops* 的 verification hierarchy，见本日 journal 自进化调研）：
   - **四级信号（强 → 弱）**：**L1 机检**（脚本/退出码/git blob sha，可复跑、与会话无关）＞ **L2 外部系统回执**（CI run 号、media_id、HTTP 返回码）＞ **L3 跨场复核**（另一场会话亲手复跑上一场的证据命令，即策略 7）＞ **L4 会话自评**（本场自己读一眼说"通过了"）。
   - **规则**：任何写进 LEDGER / PLAN / 简报的"完成/通过/达标"结论，必须至少有一条 L1–L3 级证据；**L4 只能作为补充说明，不能单独成立**。做不到就写"未验证"。
   - **理由（本场当场自证，不是引申）**：本场 `06-review.md` 的 AI 味刀里，我凭印象写了"草稿加粗 9 处"——这是 L4。随后实跑 `grep -o '\*\*[^*]*\*\*' 05-draft.md | wc -l` 得 **14 处**（L1），当场证伪。**一条 L4 结论在同一场里活了不到十分钟**，而它已经被写进了审稿记录。若无人复跑，这个数就会以"审稿结论"的身份永久留在账本里。
   - **它解释了本仓一串旧账**：W-008 的"验收判定被自产条目点亮"（L4 自评被当成验收）、L-043 的"6 条教训判定读不到"（漏的是 L1 机检的守恒检查）、L-046 的"验收动作靠记性"（本质是把 L1 降级成了 L4）。论文给这类失效起了名字：**self-confirming loop**（评估者强化自己的判断），并指出自改进强度严格跟随信号层级。
   - **可判定指标**：此后每条 LEDGER 的"完成/通过"类条目须能指出其证据属于哪一级；反向体检——凡结论句里没有命令输出、run 号、commit hash、文件路径四者之一的，即为 L4 裸奔，必须回填或改写成"未验证"。

## 当前假设（待验证）

- H1：早晚各 ≤30 分钟的会话预算足以推进本目标（若连续 3 天任务积压，此假设证伪，应在简报中提出调整）
- H4（2026-08-10 新增）：30 分钟会话预算内能跑完 aiwriter2 的锦标赛 + 七刀审稿并产出 1 篇成稿。这是 v2"每天 1 篇"的隐含前提，尚未验证——T-010 第一次端到端跑通时实测耗时；若显著超出，须在简报中向人提出调整（拆成早场出稿/晚场审稿两段，或降低单篇工序）
- H2：飞书 bot 消息链路稳定可靠（连续送达 7 天即验证）
- H3：静态仪表盘（每晚重建一次）的数据新鲜度足够（若人多次在简报外主动询问状态，此假设证伪）
